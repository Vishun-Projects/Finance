import { AppRouteLoader } from '@/components/feedback/app-route-loader';

export default function InvestmentsLoading() {
  return <AppRouteLoader variant="investments" title="Loading investments" />;
}
