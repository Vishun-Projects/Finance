// Re-exports from unified hooks file for backwards compatibility.
// All actual implementations live in hooks.ts which handles cookie-based auth correctly.
export { useLogin, useUser } from './hooks';
