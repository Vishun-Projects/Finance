import { AppRouteLoader } from '@/components/feedback/app-route-loader';

export default function TransactionsLoading() {
  return <AppRouteLoader variant="transactions" title="Loading transactions" />;
}
