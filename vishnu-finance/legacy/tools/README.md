This directory will hold the previous `tools/` implementation after migration.

Action:
- Move the current `vishnu-finance/tools/` directory here to `vishnu-finance/legacy/tools/`.
- Do not modify contents; they serve as a fallback for the master parser until fully ported.


