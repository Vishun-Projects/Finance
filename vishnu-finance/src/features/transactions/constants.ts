/** Rows fetched per request — keep small to reduce DB/browser load. */
export const TRANSACTION_PAGE_SIZE = 50;

/** Hard cap enforced on API routes. */
export const TRANSACTION_PAGE_SIZE_MAX = 100;
